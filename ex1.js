const velocidade = 100
const multa = 20%
const condutor = 0 
console.log( velocidade * 20 ) / 100
if(velocidade <= multa){
    console.log();
} if(velocidade > multa && condutor <= (multa + velocidade))

